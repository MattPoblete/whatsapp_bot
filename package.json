{
  "name": "whatsapp_bot",
  "version": "1.0.0",
  "description": "Demo project of a whatsapp bot with google api access",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --build",
    "start": "node index.js",
    "dev": "tsc --build && node index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "readline": "^1.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "typescript": "^5.8.3"
  }
}
